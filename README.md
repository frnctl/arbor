# arbor

Site multiliens statique **https://frnctl.github.io** — hub de liens FranceIntel (Telegram, X, Instagram, Threads, TikTok, PayPal).

## Repo GitHub

`frnctl/arbor` — GitHub Pages, branche `main`.

> Le repo s'appelait `frnctl.github.io`. Renommé en `arbor`. GitHub redirige automatiquement l'ancienne URL.

## VPS

Dossier présent sur le VPS : `/root/arbor`
Aucun service systemd — site statique servi par GitHub Pages uniquement.

## Déployer

```
edit index.html → git commit → git push origin main → live en quelques secondes
```

## Structure

```
index.html   → page unique (HTML/CSS/JS vanilla)
favicon.jpg
hero.jpeg
logo.jpg
README.md
```
